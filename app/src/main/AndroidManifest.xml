<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.walletapplicatiom">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WalletApplicatiom"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".LatestTransactions"
            android:exported="true" />
        <activity
            android:name=".Profile"
            android:exported="true" />
        <activity
            android:name=".TransactionStatement"
            android:exported="true"
            android:label="@string/title_activity_transaction_statement"
            android:theme="@style/Theme.WalletApplicatiom.NoActionBar" />
        <activity
            android:name=".SendMoney"
            android:exported="true"
            android:label="@string/title_activity_send_money"
            android:theme="@style/Theme.WalletApplicatiom.NoActionBar" />
        <activity
            android:name=".Homepage"
            android:exported="true"
            android:label="@string/title_activity_home_page"
            android:theme="@style/Theme.WalletApplicatiom.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>